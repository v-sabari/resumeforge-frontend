import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const root = path.dirname(path.dirname(fileURLToPath(import.meta.url)));
const src = fs.readFileSync(path.join(root, 'src/utils/previewPagination.js'), 'utf8');
fs.writeFileSync(
  path.join(root, 'api/_pagination.text.js'),
  `// GENERATED by scripts/build-pagination.mjs — do not edit.\n// Exports the source of utils/previewPagination.js as an inlinable string so\n// the PDF handler can re-run the EXACT pagination the live preview used and\n// stamp matching print breaks (preview/export parity).\nexport default ${JSON.stringify(src)};\n`
);
console.log('wrote api/_pagination.text.js');