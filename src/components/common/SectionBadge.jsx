export const SectionBadge = ({ children }) => (
  <span className="inline-flex items-center gap-1.5 rounded-full border border-brand-200 bg-brand-50 px-3.5 py-1 text-[10px] font-bold uppercase tracking-[0.2em] text-brand-700">
    {children}
  </span>
);
