export const SectionBadge = ({ children }) => (
  <span className="inline-flex rounded-full border border-brand-200 bg-brand-50 px-4 py-1 text-xs font-semibold uppercase tracking-[0.25em] text-brand-700">
    {children}
  </span>
);
